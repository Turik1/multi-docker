server {
    listen: 3000;

    location / {
        root /user/share/nginx/html;
        index index.html index.htm;
        # So that Nginx server works correctly when using React Router
        try_files $uri/ /index.html;
    }
}